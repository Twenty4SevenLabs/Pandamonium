# Pandamonium v1.0.63

This patch train ships five reviewed changes: the Add Plugins modal is now tabbed with dedicated navigation for installed plugin details and a URL-only GitHub intake; the composer gains a session identity selector with the identity's default model chip and a real reasoning-level display; the active workspace becomes settable from the picker and by the agent with honest errors; a governed Android emulator and ADB integration brings native device control to the allowlisted workstation bridge; and Settings gains guided help plus a replayable walkthrough for the model defaults. CT103 updates through the signed updater with rollback to v1.0.62.

## MAD-838 — Integrate native Android emulator and ADB control

### Problem

The operator runs a native Android emulator on the workstation and wanted Pandamonium to control it directly — discover it, manage it, install and exercise apps, and capture evidence — without mounting workstation storage into the server or exposing an unrestricted shell.

### Implementation

Commits `f76de60b` and `bdbd491f` (PR #222, merge `4aedd2d5`): a governed Android adapter resolves `adb`, `emulator`, `sdkmanager`, and configured AVDs through installation-owned configuration; lists connected emulators and devices with serial, model, Android/API version, boot state, and authorization state; starts, stops, reboots, and waits for AVDs; and supports bounded actions (APK install, package launch and force-stop, deep-link and intent launches, screenshots and screen recording, scoped logcat, and explicit tap/swipe/text/key input) with allowlisted argument schemas, an explicit target serial, timeouts, cancellation, and serialized duplicate actions. Arbitrary `adb shell`, arbitrary host shell, credentials, and Play Store automation stay out of scope; destructive actions require explicit confirmation.

### Product value and UI visibility

A connected Android emulator becomes a first-class development target from the governed tool surface, with progress, stdout/stderr, timeout, cancellation, and final evidence flowing through the existing task workflow. The workstation bridge boundary is preserved: no Android SDK dependency in the Pandamonium base image and no workstation filesystem mount into CT103. The boundaries are documented in `docs/android-emulator-runtime.md`.

### Verification

Contract and unit tests cover command construction, argument allowlists, device targeting, timeouts, cancellation, and hostile arguments; PR #222 checks are green. The v1.0.63 release candidate runs the full Python and Chromium suites. Headed control of the operator's workstation emulator remains the documented live acceptance path.

### Configuration, compatibility, and limitations

Additive workstation-bridge capability: no dependency added to the base image, no data or credential change, and rollback is disabling the adapter and its registration. Android SDK configuration is installation-owned; uninstall, clear-data, and wipe actions are confirmation-gated.

### References

- [Linear MAD-838](https://linear.app/madpanda3d/issue/MAD-838/integrate-native-android-emulator-and-adb-control)
- [Pull request #222](https://github.com/MADPANDA3D/Pandamonium/pull/222)
- [GitHub mirror #221](https://github.com/MADPANDA3D/Pandamonium/issues/221)

## MAD-931 — Settings: guided help for Vision, Research, Image Generation, and Voice model defaults

### Problem

The model-default sections in Settings explained nothing: the operator could not tell what each model slot does, what to pick, or how fallback, cost, and latency behave.

### Implementation

Commits `01e21e0d`, `1fed5bc9`, and `05fd3354` (PR #224, merge `4aedd2d5`): each model-default section (Utility, Vision, Research Model, Image Generation, Voice) gains a keyboard-reachable help control rendered in the existing tour/popup language, with human copy covering purpose, selection guidance, fallback behavior, and cost/latency tradeoffs, anchored to the live configured endpoints without exposing secrets. The first-run guide gains a model-defaults chapter that opens the right sections and can be replayed without resetting settings, reusing the existing tour/modal system.

### Product value and UI visibility

Operators can now understand and configure every model default in place, and replay the walkthrough from the guide without losing settings. Copy is human-readable; raw provider field names and backend codes are not shown.

### Verification

The focused settings checks and the browser spec cover help open/close and guide replay, and PR #224 checks are green. The v1.0.63 release candidate runs the full Python and Chromium suites.

### Configuration, compatibility, and limitations

Copy and tour wiring only: no new modal framework, no dependency, and no model behavior change. The M14 setup wizard remains the first-run entry point; this chapter links to the same settings sections rather than duplicating the wizard shell.

### References

- [Linear MAD-931](https://linear.app/madpanda3d/issue/MAD-931/settings-guided-help-for-vision-research-image-generation-and-voice)
- [Pull request #224](https://github.com/MADPANDA3D/Pandamonium/pull/224)
- [GitHub mirror #223](https://github.com/MADPANDA3D/Pandamonium/issues/223)

## MAD-883 — Workspace: make active workspace settable and visible to the agent

### Problem

The operator reported there was no way to set the active workspace to work out of. The picker and slash command existed, but failures looked like an absent feature, the setting lived only in one browser's local storage, and the in-app agent had no way to set, clear, or report the workspace.

### Implementation

Commits `a50984da` and `03bb21b2` (PR #214, merge `3eba6956`): the active workspace persists per chat session and the picker surfaces honest errors instead of a generic browse failure, and a new `manage_workspace` tool lets the agent set, clear, and report the active workspace with server validation through the existing confinement path. Commit `8c970dc6` makes the picker spec deterministic in CI.

### Product value and UI visibility

The workspace pill, overflow picker, and `/workspace set` now agree on the per-session active workspace, and agents can change it when asked. Errors tell the operator what actually failed (authentication, missing folder, or unsupported path) rather than implying the feature is missing.

### Verification

The focused picker, workspace, and agent-tool checks pass, the deterministic browser spec is stable in CI, and PR #214 checks are green. The v1.0.63 release candidate runs the full Python and Chromium suites.

### Configuration, compatibility, and limitations

No dependency or database change; the workspace confinement semantics (`vet_workspace` and path resolvers) are unchanged. Workspace state remains per chat session, and unauthenticated or non-admin sessions still cannot browse host folders by design.

### References

- [Linear MAD-883](https://linear.app/madpanda3d/issue/MAD-883/workspace-make-active-workspace-settable-and-visible-to-the-agent)
- [Pull request #214](https://github.com/MADPANDA3D/Pandamonium/pull/214)
- [GitHub mirror #213](https://github.com/MADPANDA3D/Pandamonium/issues/213)

## MAD-930 — Composer: identity selector, identity-default model chip, and real reasoning level

### Problem

The composer's left chip showed the identity name but could not switch it, there was no visible link between an identity and the model it loads, and the reasoning chip said "Default" instead of the level the selected model is actually set to.

### Implementation

Commits `4c82c5c3`, `6023603f`, and `f7ec7913` (PR #218, merge `707e35fc`): the identity chip becomes a session identity selector, a new chip to its right loads and overrides the selected identity's attached default model for the session without mutating the saved identity profile, and the reasoning chip resolves the real level from the existing reasoning-level path instead of the literal "Default".

### Product value and UI visibility

Switching identity immediately updates the model chip to that identity's default and the reasoning chip to the real level; overriding the model in the composer applies to the current session only. The capability-aware footer and keyboard/mobile behavior from the existing selector are preserved.

### Verification

The new composer source and browser checks cover identity switching, per-session model override, and the reasoning label, and the existing selector acceptance specs still pass. PR #218 checks are green; the v1.0.63 release candidate runs the full Python and Chromium suites.

### Configuration, compatibility, and limitations

Composer display and session routing only: no provider or endpoint contract changes, no new dependencies. Saved identity profiles are not modified by a session override.

### References

- [Linear MAD-930](https://linear.app/madpanda3d/issue/MAD-930/composer-identity-selector-identity-default-model-chip-and-real)
- [Pull request #218](https://github.com/MADPANDA3D/Pandamonium/pull/218)
- [GitHub mirror #217](https://github.com/MADPANDA3D/Pandamonium/issues/217)

## MAD-946 — Plugins: split Add Plugins into Installed and Add tabs with dedicated plugin-detail navigation

### Problem

The Add Plugins modal stacked search, Add from GitHub, the scan panel, the installed list, and plugin details into one surface. Clicking an installed plugin opened a side-by-side detail column, and the Add-from-GitHub form still asked for a ref (`HEAD`) that the operator never wants to type.

### Implementation

Commit `95c47c3e` (PR #220, merge `dba7e956`): the modal is split into **Installed plugins** (default) and **Add a new plugin** tabs. The installed list is full width and a click opens the plugin details as their own view with a persistent "← Back to installed" button; no side-by-side column remains. The Add tab keeps marketplace browse (search, categories, refresh, catalog list and detail) and a URL-only GitHub intake — the ref input is removed, scans resolve the default-branch HEAD, and installs still pin the scanned revision. The idle scan card no longer leaks through the `hidden` attribute.

### Product value and UI visibility

Installed plugins are the first thing the operator sees, and details are reachable without sharing the screen; adding a plugin is one pasted URL and a scan. Tab switching is keyboard reachable (arrow keys and standard tab semantics), and the mobile layout keeps the dedicated back navigation.

### Verification

Twenty-two focused Chromium specs pass across the marketplace, installed, intake, wizard empty-state, dock-freeze, and modal-lifecycle suites, asserting tab switching, the installed detail/back navigation, the absent ref input, and the approval flow inside the Add tab. The full Chromium suite passed 150/150, and PR #220 checks are green.

### Configuration, compatibility, and limitations

UI layout and navigation only: no API, registry, scanner, installer, or authority change, and no new dependencies. Approval previews, scan progress, and installed-detail content are unchanged as information.

### References

- [Linear MAD-946](https://linear.app/madpanda3d/issue/MAD-946/plugins-split-add-plugins-into-installed-and-add-tabs-with-dedicated)
- [Pull request #220](https://github.com/MADPANDA3D/Pandamonium/pull/220)
- [GitHub mirror #219](https://github.com/MADPANDA3D/Pandamonium/issues/219)
